import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const Color lightPrimary = Color(0x000DCDAA);
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightOnPrimary = Color(0xFFFFFFFF);
  static const Color lightOnBackground = Color(0xFF000000);
  static const Color lightOnSurface = Color(0xFF000000);
  static const Color lightAppBar = Color(0xFFF5F5F5);
  static const Color lightCard = Color(0xFFE0E0E0);

  // Dark Theme Colors
  static const Color darkPrimary = Color(0xFFBB86FC);
  static const Color darkBackground = Color(0xFF121212); // Main dark background
  static const Color darkSurface = Color(
      0xFF1E1E2C); // Slightly lighter surface (like cards, appbar background)
  static const Color darkOnPrimary = Color(0xFF000000);
  static const Color darkOnBackground = Color(0xFFFFFFFF);
  static const Color darkOnSurface = Color(0xFFFFFFFF);
  static const Color darkAppBar =
      Color(0xFF2D2D44); // AppBar specific dark color
  static const Color darkCard = Color(0xFF2D2D44); // Card specific dark color
}
